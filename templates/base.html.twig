<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}ᗰᗩᖇ1ᖴOOT{% endblock %}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- FontAwesome CDN for Icons -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 text-gray-800 flex flex-col min-h-screen">

    <!-- Header -->
    <header class="bg-gray-800 text-white py-4 shadow">
        <div class="container mx-auto flex justify-between items-center px-4">
            <h1 class="text-xl font-bold flex items-center">
             ᗰᗩᖇ1ᖴOOT
            </h1>
            <nav class="flex items-center space-x-4">
                {% if app.user %}
                    <form action="{{ path('app_logout') }}" method="post" class="inline">
                        <input type="hidden" name="_csrf_token" value="{{ csrf_token('logout') }}">
                        <button type="submit" class="text-red-500 hover:text-red-600 font-semibold flex items-center">
                            <!-- Icône de déconnexion -->
                            <i class="fas fa-sign-out-alt mr-2"></i> Déconnexion
                        </button>
                    </form>
                {% else %}
                    <a href="{{ path('app_login') }}" class="text-blue-400 hover:text-blue-500 font-semibold flex items-center">
                        <!-- Icône de connexion -->
                        <i class="fas fa-sign-in-alt mr-2"></i> Connexion
                    </a>
                    <a href="{{ path('app_register') }}" class="text-green-400 hover:text-green-500 font-semibold flex items-center">
                        <!-- Icône d'inscription -->
                        <i class="fas fa-user-plus mr-2"></i> Inscription
                    </a>
                {% endif %}
            </nav>
        </div>
    </header>

    <!-- Main Content -->
    <main class="container mx-auto py-8 px-4 flex-grow">
        {% block body %}
        <p class="text-center text-gray-600">Bienvenue sur notre plateforme de réservation de terrains !</p>
        {% endblock %}
    </main>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white py-4 mt-auto">
        <div class="container mx-auto text-center flex items-center justify-center">
            <p>&copy; {{ "now"|date("Y") }} Réservation de Terrains. Tous droits réservés.</p>
            <div class="ml-4 flex space-x-4">
                <!-- LinkedIn Icon -->
                <a href="https://www.linkedin.com/" class="text-blue-400 hover:text-blue-500 flex items-center">
                    <i class="fab fa-linkedin-in mr-2"></i> LinkedIn
                </a>
                <!-- Instagram Icon -->
                <a href="https://www.instagram.com/" class="text-pink-400 hover:text-pink-500 flex items-center">
                    <i class="fab fa-instagram mr-2"></i> Instagram
                </a>
                <!-- Facebook Icon -->
                <a href="https://www.facebook.com/" class="text-blue-600 hover:text-blue-700 flex items-center">
                    <i class="fab fa-facebook-f mr-2"></i> Facebook
                </a>
            </div>
        </div>
    </footer>

</body>
</html>
