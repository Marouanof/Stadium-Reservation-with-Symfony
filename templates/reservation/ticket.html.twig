<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Ticket de réservation #{{ reservation.id }}</title>
    <style>
        body { font-family: Arial, sans-serif; background: #f4f4f4; padding: 20px; }
        .ticket {
            max-width: 600px;
            margin: auto;
            background: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-top: 5px solid #007bff;
        }
        .header {
            text-align: center;
            margin-bottom: 20px;
        }
        .header img {
            max-width: 150px;
            margin-bottom: 10px;
        }
        h1 { color: #007bff; font-size: 20px; }
        .details {
            padding: 10px;
            border-top: 2px solid #ddd;
            border-bottom: 2px solid #ddd;
            margin: 20px 0;
        }
        .details p { font-size: 14px; margin: 5px 0; }
        .qr-code {
            text-align: center;
            margin-top: 20px;
        }
        .footer {
            text-align: center;
            font-size: 12px;
            color: #666;
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <div class="ticket">
        <div class="header">
            <img src="{{ asset('images/mar1foot-logo.png') }}" alt="Mar1Foot ">
            <h1>Ticket de réservation</h1>
            <p>#{{ reservation.id }}</p>
        </div>

        <div class="details">
            <h2>Détails de la réservation</h2>
            <p><strong> Terrain:</strong> {{ reservation.terrain.name }}</p>
            <p><strong> Adresse:</strong> {{ reservation.terrain.adresse }}</p>
            <p><strong> Début:</strong> {{ reservation.dateDebut|date('d/m/Y H:i') }}</p>
            <p><strong> Fin:</strong> {{ reservation.dateFin|date('d/m/Y H:i') }}</p>
            <p><strong> Prix:</strong> {{ reservation.terrain.prix }}€</p>
        </div>


        <div class="footer">
            <p>Merci d'avoir réservé avec <strong>Mar1Foot</strong> !</p>
            <p> Contact : support@mar1foot.com |  +212 689-213-345</p>
        </div>
    </div>
</body>
</html>
